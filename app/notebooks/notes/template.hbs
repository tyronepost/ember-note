<div class="col-md-12">
  Notes
</div>
<div>
  <div class="col-md-4">
    <div>
      title: {{input class="title" value=title}} <button class="add-note" {{action 'addNote'}}>Add</button>
    </div>
    <div>
      <ul>
        {{#each model as |note|}}
            {{#link-to 'notebooks.notes.note' note.id}}
            <li data-test-note={{note.id}}>{{note.title}}</li>
            {{/link-to}}
            <button {{action 'deleteNote' note}}>delete</button>
        {{/each}}
      </ul>
    </div>
  </div>
  <div class="col-md-8">
    {{outlet}}
  </div>
</div>
